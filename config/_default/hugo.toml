baseURL      = "https://samkang.github.io/mola-outputs/"
languageCode = "zh-tw"
title        = "mola-outputs 🐟"

defaultContentLanguage = "zh-tw"
hasCJKLanguage = true

theme = "hugo-theme-stack"

[pagination]
    pagerSize = 5

[permalinks]
    # 改回比較直覺的 /posts/檔名/ 格式
    post = "/posts/:slug/"
    page = "/:slug/"
